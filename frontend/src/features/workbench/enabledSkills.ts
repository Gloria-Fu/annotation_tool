import type { SkillName } from "./skillDefinitions";

// Availability only: keep all implemented templates in skillDefinitions.ts.
// "Pick", "Place", "Grasp", "Push", "Pull"
export const ENABLED_SKILLS: readonly SkillName[] = ["Pick", "Place"];
